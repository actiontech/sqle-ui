export type Theme = {
  common: {
    padding: number;
    color: {
      warning: string;
    };
  };
  loginFormBg: string;
  header: {
    background: string;
    color: string;
  };
  editor: {
    border: string;
  };
};
