export type Theme = {
  common: {
    padding: number;
    color: {
      warning: string;
      disabledFont: string;
    };
  };
  loginFormBg: string;
  header: {
    background: string;
    color: string;
  };
  editor: {
    border: string;
  };
};
